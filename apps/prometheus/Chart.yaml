apiVersion: v2
name: prometheus
description: Prometheus — metrics collection (GitOps wrapper, required by OpenCost + Grafana)
type: application
version: 0.1.0
appVersion: "v3.x"
dependencies:
  - name: prometheus
    version: "27.0.0"
    repository: "https://prometheus-community.github.io/helm-charts"
