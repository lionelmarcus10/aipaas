apiVersion: v2
name: vllm
description: vLLM — LLM inference server (GitOps wrapper, k3d CPU / AWS GPU switch)
type: application
version: 0.1.0
appVersion: "latest"
dependencies:
  - name: vllm
    version: "0.1.0"
    repository: "https://vllm-project.github.io/vllm-helm"
