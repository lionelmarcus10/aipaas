apiVersion: v2
name: grafana
description: Grafana — Dashboard unifié (GitOps wrapper)
type: application
version: 0.1.0
appVersion: "11.x"
dependencies:
  - name: grafana
    version: "8.0.0"
    repository: "https://grafana.github.io/helm-charts"
