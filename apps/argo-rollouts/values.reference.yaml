# Argo Rollouts Helm values reference — k3d / AWS switch
# Actual values are inlined in infra/live/003_argocd_config/terragrunt.hcl

controller:
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
dashboard:
  enabled: true
