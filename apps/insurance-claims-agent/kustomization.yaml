# Kustomize base — k3d (gVisor + FAISS + local-path)
# ArgoCD lit ce fichier et applique deployment.yaml tel quel.
# Pour EKS: pointer ArgoCD vers apps/insurance-claims-agent/eks/
resources:
  - deployment.yaml
